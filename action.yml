name: Publish site
description: Publish your site to a website
author: a-rvid
branding:
  icon: upload-cloud
  color: white

inputs:
  url:
    description: "URL or IP to send the data"
    required: false
  ip:
    description: "IP to send the data"
    required: false
  privkey:
    description: "Client Private key"
    required: false 
  cert:
    description: "Client certificate (pubkey)"
    required: false
  dir:
    description: "The directory to publish"
    required: true

runs:
  using: "composite"
  steps:
    - run: ${{ github.action_path }}/deploy.sh 
      env:
        URL: ${{inputs.url}}
        IP: ${{inputs.ip}}
        TARGET: ${{inputs.dir}}
        PRIVKEY: ${{inputs.privkey}}
        CERT: ${{inputs.cert}}
      shell: bash
